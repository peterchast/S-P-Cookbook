---
layout: layouts/base.njk
section: post
---

<article>

  <section class="recipe-intro">
    <div class="wide-wrapper flow">
      <h1 class="recipe-name">{{ title }}</h1>
      <h3 class="tagline">{{ tagline }}</h3>
      {% if tags %}
        <ul class="tag-list" role="list">
          {% for tag in tags %}
            {%- if tag != "post" -%}
              <li>
                {% set tagUrl %}/tags/{{ tag }}/{% endset %}
                <a class="tag smaller-text special-a" href="{{ tagUrl | url }}" rel="tag">#{{ tag }}</a>
              </li>
            {%- endif -%}
          {% endfor %}
        </ul>
      {% endif %}
    </section>

    <section class="recipe-featured">
      <div class="featured-img medium-wrapper">
        <img src="{{ featured_image}}">
      </div>
    </section>

    <section class="recipe-about">
      <div class="narrow-wrapper">
        {% if story.length %}
          <div class="recipe-commentary flow">
            {{ story  | markdownify | safe  }}
            </div>
        {% endif %}

      <div class="recipe-facts">
        <div class="stats info-box">
          <div class="stats-active">
            <span class="label">
              <p>Active Time:</p>
            </span>
            <span class="data">
              <h3>{{ active_time }}</h3>
            </span>
          </div>
        </div>
        <div class="stats info-box">
          <div class="stats-time">
            <span class="label">
              <p>Total Time:</p>
            </span>
            <span class="data">
              <h3>{{ total_time }}</h3>
            </span>
          </div>
        </div>
  <div class="stats info-box">
    <div class="stats-yield">
    <span class="label">
      <p>Yield:</p>
    </span>
    <span class="data">
      <h3>{{ yield }}</h3>
    </span>
    </div>
  </div>
</div>
</div>
</section>

<section class="recipe-ingredients flow">
  <div class="narrow-wrapper flow">
    <h2>Ingredients</h2>
      {% for groups in ingredients_list %}
      <div class="recipe-component flow">
      <h4 class="highlight"> {% if groups.group_name != "Ingredient List" %} {{ groups.group_name }} {% endif %}</h4>
      <ul>
        {% for items in groups.ingredient %}
          <li>
            <span class="quant">{{ items.amount }}</span> <span class="unit">{{ items.unit }}</span> <span class="desc">{{ items.ingredient }}</span>
          </li>
        {% endfor %}
      </ul>
      </div>
      {% endfor %}

{% if special_equipment.length %}
<div class="special-equipment flow">
<h4 class="highlight">Special equipment you'll need 🔪🍳:</strong></h4>
<ul>
  {% for items in special_equipment %}
  <li>
    {{ items.equipment }}
  </li>
  {% endfor %}
</ul>
</div>
{% endif %}

</section>

<section class="recipe-instructions">
  <div class="narrow-wrapper flow">
  <ol role="list" class="instructions-list flow">
  {% for steps in instructions %}
    <li>
      <div class="single-instruction flow">
      <h3 class="step-title"> {{ steps.title }}</h3>

      {% if steps.ingredients_used.length %}
      <ul class="step-needed">
        {% for items in steps.ingredients_used %}
          <li>
            <span class="quant">{{ items.amount }}</span> <span class="unit">{{ items.unit }}</span> <span class="desc">{{ items.ingredient }}</span>
          </li>
        {% endfor %}
          </ul>
        {% endif %}

      <p class="step-text">{{ steps.description }}</p>

      {% if steps.instruction_photo %}
      <div class="step-photos">
          <img src="{{ steps.instruction_photo }}">
        </div>
      {% endif %}


{% if steps.instruction_tip.length  %}
 <div class="step-tips">
 <div>
        <label for="toggle_{{ loop.index }}" class="show-tip"><div class="tip-activate">
          <p class="smaller-text">Just The Tip</p>
          <svg class="tip-arrow" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
  <circle cx="8" cy="8" r="8" fill="inherit"></circle>
  <path d="M10.666 11.4075L4.74009 11.4075L4.74009 5.48157" stroke="#1B4D7A"></path>
  <path d="M4.73958 11.4076L11.4062 4.74097" stroke="#1B4D7A"></path>
              </svg>
            </div>
          </label>
          </div>
          <input type="checkbox" id="toggle_{{ loop.index }}">
          <div class="tip-inner flow">
   {% for items in steps.instruction_tip %}</h4>
       <h4 class="step-title">{{ items.title }}</h4>
       <p>{{ items.description}}</p>
       {% if items.tip_photo.length %}
       <div class="step-photos">
         <img src="{{ items.tip_photo }}">
         </div>
         {% endif %}

   {% endfor %}


{% endif %}

    </li>
  {% endfor %}
  <li>
  <div class="get-some flow">
    <h3 class="step-title">Get Some!</h3>
    <p class="step-text">You just made a recipe! I'm so proud of you! It's time to honor your dilligence and perserverance by consuming the fruits (or vegetables, et al.) of your labor.</p>
    <p><em>What's that? You made a dessert to share with friends at a dinner party? Take a big bite anyway!<em></p>
    </div>
  </li>
</ol>
</div>
</section>

  {% if other_notes.length or inspired_by.length %}
<section class="recipe-footer flow">
  <div class="narrow-wrapper flow">
    <h3 class="step-title">More Recipe Notes</h3>
      <ul class="step-text">
        {% for items in other_notes %}
        <li>
          {{ items.note }}
        </li>
        {% endfor %}
        <li>
        <div class="flow inspired-by">
          <p class="strong">This recipe was inspired by:</p>
          &nbsp;{{ inspired_by | markdownify | safe }}
    </li>
      </ul>
    </div>
    {% endif %}
</section>

</article>
